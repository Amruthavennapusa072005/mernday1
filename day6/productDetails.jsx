const ProductDetails=()=>{
    const (id)=useParams()
    const products=[
        {
            id:1,name:'mobile',desc:'with call feature'),
            {id:2,name:'laptop',desc:'with high specs'},
            {id:3,name:'keyword',desc:'with all keys'},
        }
        const product=products.find(p)=>{p.id===}
    ]
    return(
        <div>
            <h1 className="font-extrabold w-100 center">Product</h1>
            <link className="🟦text-blue-400" to={'/services/${product.id'}>view description </link>
            </>
            ))}
        </div>
    
